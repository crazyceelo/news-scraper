<nav class="navbar navbar-inverse">
    <div class="container">
        <div class="row">
            <div class="col-md-1">
                <input id="scrape-button" class="btn btn-default navbar-btn" type="button" value="Scrape it">
            </div>
            <div class="col-md-2">
                <input id="display-button" class="btn btn-default navbar-btn" type="button" value="Display News">
            </div>
        </div>
    </div>
</nav>

<body>
    <div class="container">
        <div class="row">
            {{#each articles}}
            <div class="col-md-12">
                <a href={{this.link}} target="_blank" ><h1 id="articles">{{this.title}}</h1></a>
            </div>
            {{>form}}
            {{/each}}
        </div>
    </div>
    <script src="../assets/js/app.js"></script>
</body>

